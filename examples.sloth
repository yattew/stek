example programmes
// for printing the table of 5
0 while 1 + dup 11 < do
    dup 5 * top pop
end
pop

// for printing even numbers from 1 to 10
0 while 1 + dup 11 < do
    if dup 2 % 0 == do
        top
    end
end
pop