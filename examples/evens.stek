"std.stek" include
function evens do
    var till
    ++ till set
    0 while ++ dup till val < do
        if dup 2 % 0 == do
            dup println
        end
    end pop
    null till set
end
10 evens